# Loaded ONLY by `next dev` (NODE_ENV=development). Never used by `next build`,
# so it cannot leak localhost URLs into the packaged Electron production build.
#
# The Cloud Vault now serves auth, Azure tokens AND GPT-4o diarization.
NEXT_PUBLIC_API_URL=http://localhost:8001
